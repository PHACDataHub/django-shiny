apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: djangoapp-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: standard-rwo
