{% extends 'base.jinja' %}

{% block content %}

  <div class="container mt-3">
    <h2 class="mb-3">R Shiny app directory</h2>
    {# <p class="mt-3">
      <b>Click one of the Shiny app links below.</b> If the app is protected (<i class="bi bi-lock text-muted"></i>), you will be prompted to login.
    </p> #}
    {% set default_card_link = "a.view-app" %}
    {% include 'components/app_list.jinja' %}
    <hr class="mt-5">
    <h4>Technical info</h4>
    <p class="mt-3">
      Each Shiny app runs in its own container and is proxied through Django, appearing in an iframe.
    </p>
    <p>
      For more information, or to add another Shiny app, see 
<a href="https://github.com/PHACDataHub/django-shiny"><i class="bi bi-github"></i> django-shiny</a> on GitHub.
    </p>
  </div>
{% endblock %}
