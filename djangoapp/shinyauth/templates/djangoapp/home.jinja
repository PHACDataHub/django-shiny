{% extends 'base.jinja' %}

{% block content %}

  <div class="container mt-3">
    <h2>R Shiny app directory</h2>
    <p class="mt-3">
      <b>Click one of the Shiny app links below.</b> If the app is protected (<i class="bi bi-lock text-muted"></i>), you will be prompted to login.
    </p>
    <ul>
      {% for app in apps %}
        <li>
          <a href="{{ url("shiny", app.slug) }}">{{ app|capitalize }}</a>
          {% if app.is_admin_only %}
            <i class="bi bi-shield-lock text-danger" title="Admin access only"></i>
          {% elif not app.is_public %}
            <i class="bi bi-lock text-muted" title="Limited access"></i>
          {% else %}
            <i class="bi bi-unlock text-muted" title="Public access"></i>
          {% endif %}
        </li>
      {% endfor %}
    </ul>
    <hr class="mt-5">
    <h4>Technical info</h4>
    <p class="mt-3">
      Each Shiny app runs in its own container and is proxied through Django, appearing in an iframe.
    </p>
    <p>
      For more information, or to add another Shiny app, see 
<a href="https://github.com/PHACDataHub/django-shiny" class="text-body"><i class="bi bi-github"></i> django-shiny</a> on GitHub.
    </p>
  </div>
{% endblock %}
