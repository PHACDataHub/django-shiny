{% extends 'base.jinja' %}

{% block content %}

  <div class="container mt-3">
    <h2>Authentication for R Shiny apps</h2>
    <p class="mt-3">
      <b>To begin, try clicking one of the links above.</b> If the app is protected (<i class="bi bi-lock text-muted"></i>), you will be prompted to login.
    </p>
    <hr>
    <p>
      Each Shiny app runs in its own container and is proxied through Django, appearing in an iframe.
    </p>
    <p>
      By modifying the <code>shiny_apps.json</code> file, you can add Shiny apps to this page.
      For more information, see the <a href="https://github.com/PHACDataHub/django-shiny">django-shiny GitHub repo</a>.
    </p>
  </div>
{% endblock %}
